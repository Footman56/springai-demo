spring:
  application:
    name: demo

  ai:
    deepseek:
      api-key: ${DEEPSEEK_API_KEY}
      chat:
        options:
          model: deepseek-chat
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
